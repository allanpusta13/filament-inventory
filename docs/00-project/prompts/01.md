# Prompt 1 — Project Setup

I'm building a multi-warehouse inventory system with Laravel 13 and Filament v5.
Set up a fresh Laravel 13 project, install Filament v5 with the admin panel,
and create the first admin user. Give me exact commands in order, plus
anything I need to check afterward to confirm the panel loads at /admin.
