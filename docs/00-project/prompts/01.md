## 📂 Phase 01: Pre-Build Structural Audit & Baseline Refactoring

### Developer Instruction Prompt
Initialize the system lifecycle by executing a baseline index and code audit. Locate legacy flat directories, identify duplicate schema definitions (DRY violations), detect performance bottlenecks (N+1 database queries), and classify test suites for realignment:

1. **Graphify Indexing**: Run `npx graphify update` to refresh the visual model node graph.
2. **Relocation Mapping**: Map all misplaced Filament components from flat directories to the v5 nested structure.
3. **Obsolete Pruning**: Scan the test folders and identify files referencing defunct features or models. **Pruning checkpoint**: Present a list of dead-reference files to the user before deleting them from disk.

```bash
# Verify graphify updates cleanly
npx graphify update
# Run Pint to enforce coding style guidelines
./vendor/bin/pint --test
```