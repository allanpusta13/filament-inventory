# Prompt 3 — Roles & Permissions

Add a simple native role system to my Laravel 13 app (no package). I have a
'role' column on users, values 'admin' or 'warehouse_staff'. Create:
1. A UserRole PHP enum (Admin, WarehouseStaff) and cast it on the User model
2. isAdmin() and canAccessWarehouse(Warehouse $warehouse) helper methods on User
3. A users.warehouses() belongsToMany relationship (pivot: user_warehouse)
Show the full User model with these additions.
