## 📂 Phase 14: Automated CI/CD Testing & E2E Validation Suite

### Developer Instruction Prompt
Write the comprehensive automation suite and deployment configuration to verify and protect codebase integrity across all operational features:

1. **GitHub Actions Workflow (`.github/workflows/ci.yml`)**:
   * Create a deployment workflow triggering on every pull request or push to master.
   * Spin up service containers for **PostgreSQL** and **Redis**.
   * Run the test suite under strict environment isolation (SQLite in-memory configurations for rapid Pest unit checks, PostgreSQL for browser checks).
2. **Database Reset Hooks**:
   * In the base E2E testing framework, configure a `beforeEach` test hook that executes shell execution commands (`php artisan migrate:fresh --seed --class=DemoWorkflowSeeder`) to completely refresh the database state before every browser test run.
3. **Comprehensive Pest Test Specs**:
   * Write features tests to assert that:
     * High-precision unit cost overrides are correctly computed on shortage calculations.
     * Manual adjustments containing compliance notes under 15 characters are successfully blocked.
     * Direct transfers instantly debit the origin and credit the destination in a single atomic transaction.
     * Deleting a `confirmed` transfer requisition automatically releases origin warehouse stock reservations.
     * Scanning a dispatched QR code on an unauthorized device throws a strict `403` permission error.

---

## 🧪 Unified Success Criteria & Verification (Binary Outcomes)

An execution run is considered successful if and only if:
1. `vendor/bin/pest` executes with 100% passing tests (SQLite isolated unit tests, and PostgreSQL Dusk checks).
2. `npx playwright test` completes with 0 failures on sequential runs.
3. The visual graph index `npx graphify update` compiles with zero unmapped models or resources.
4. No database schema contains a cached balance table like `warehouse_stocks`. All stock lookups are derived at runtime.
5. All wizard forms are verified to reside inside `3xl` width Dialog Modals rather than custom page routes.

---

## 📋 Required Final Outputs

The autonomous execution report must output:
1. **Directory Tree Differences**: Visual comparison confirming that all files reside at their exact nested path specs.
2. **Performance Benchmarks**: Query execution times on heavy lists (e.g. `StockMovement`) confirming N+1 loops are eliminated.
3. **Pest & Playwright Logs**: Clean, passing output screens showing total cases compiled.
4. **Deviations Log**: Detailed technical explanations of any structural alterations made during the clean-up.
